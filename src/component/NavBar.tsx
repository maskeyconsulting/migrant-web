
















}  );    </nav>      </Link>        {t('countries')}      <Link href={`/${locale}/countries`}>      </Link>        {t('home')}      <Link href={`/${locale}`}>    <nav>  return (    const t = useTranslations('navigation');export default function NavBar({ locale }: { locale: string }) {import Link from 'next/link';import { useTranslations } from 'next-intl';